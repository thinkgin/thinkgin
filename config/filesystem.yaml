# 文件系统配置
#
# NOTE: 本版本**未实现**文件存储抽象层。若业务需要上传文件，推荐直接使用
# 官方 SDK（aliyun-oss-go-sdk / aws-sdk-go-v2 等），或者用 os.WriteFile +
# http.FileServer 组合处理本地磁盘。
#
# 保留空骨架是为了将来加入 Storage 抽象时配置结构的稳定性。
filesystem: {}
