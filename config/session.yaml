# Session 配置
session:
  # 驱动类型：memory（默认，单进程）或 redis（多实例共享）。
  driver: "memory"

  # Cookie 名称
  name: "THINKGIN_SESSION"

  # 生存时间（秒）
  lifetime: 7200

  # 使用 redis driver 时引用的连接（对应 database.yaml 的 connections.<name>）
  redis:
    connection: "redis"

  # Cookie 安全属性
  cookie:
    path: "/"
    domain: ""
    secure: false
    http_only: true
    same_site: "lax" # lax / strict / none
